{
  "framework": "vite",
  "buildCommand": "npm install --no-save @emailjs/browser && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 